#include "ros/ros.h"
#include "std_msgs/Float32MultiArray.h"

float posX;
float posY;

void faceCenterCallback(const std_msgs::Float32MultiArray::ConstPtr& msg) {
	//msg[0] -> posX;
	//msg[1] -> posY;
	//ROS_INFO("I heard: [%s]", msg->data.c_str())
}

int main (int argc, char **argv) {

	ros::init(argc, argv, "cambot_subscriber");

	ros::NodeHandle node;

	ros::Subscriber sub = node.subscribe("face_center_xy", 1, faceCenterCallback);
	
	while (ros::ok()) {
	
		ros::spinOnce();
	
	}

	return 0;
}


  

